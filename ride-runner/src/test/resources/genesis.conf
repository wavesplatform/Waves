# Configuration for genesis block generator
# To generate run from SBT: node/runMain com.wavesplatform.GenesisBlockGenerator ride-runner/src/test/resources/genesis.conf
genesis-generator {
  network-type = "N"

  # base-target = 153722867
  average-block-delay = 60s
  # timestamp = 1500635421931 # Comment to use the current time

  # seed text -> share
  # the sum of shares should be <= initial-balance
  distributions = [
    { seed-text = "foo", nonce = 0, amount = 3732000000000000 }
    { seed-text = "bar", nonce = 1, amount = 2053000000000000 }
    { seed-text = "baz", nonce = 2, amount = 4215000000000000 }
  ]
}
