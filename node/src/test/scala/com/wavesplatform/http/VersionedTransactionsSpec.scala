package com.wavesplatform.http

import com.wavesplatform.TransactionGen

class VersionedTransactionsSpec extends RouteSpec("/transactions") with TransactionGen {
  "Versioned transactions handled by route" - {
    "TransferTransaction" in {

    }
  }
}
