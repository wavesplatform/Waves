waves {
  blockchain {
    type = MAINNET
  }

  rest-api {
    bind-address = "0.0.0.0"
    port = 6890
  }

  ride-runner {
    directory = ${WVDATA}
    enable-traces = no
  }
}

include "local.conf"
