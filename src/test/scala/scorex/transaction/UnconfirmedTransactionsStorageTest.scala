package scorex.transaction

import org.scalatest.FunSuite

class UnconfirmedTransactionsStorageTest extends FunSuite {

  ignore("clearIncorrectTransactions() removes valid but expired txs") {
  }

  ignore("clearIncorrectTransactions() removes not expired but invalid txs") {
  }

  ignore("packUnconfirmed() packs txs in correct order") {
  }
}